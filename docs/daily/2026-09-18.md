---
title: 2026-09-18 工作日报
description: quant-risk 今日会话记录 — 港股负债率修复、Skill 分发重构、V1.7 发布、OpenKnowledge 接入 Kimi Code
tags: [日报, quant-risk, 2026-09-18, 发布]
---

# 2026-09-18 工作日报

> 今日主线：bug 修复 → Skill 分发重构 → 文档重写 → 发布 V1.7 → 知识库接入。

## 一、今日完成

### 1. A股+港股每日推荐（两轮）
- 早盘 + 盘中各跑一轮 `daily_run.py`，最终推荐：A股 **跨境通 002640 / 金石亚药 300434**（当前可布局）；港股 **峰岹科技 01304 / 东亚银行 00023**（当前可布局），其余 16 只 WATCH。
- 报告落盘 `report/recommend-{cn,hk}-20260918-daily.md`。

### 2. 修复：腾讯港股 78 字段负债率误映射（commit `1434a3b`）
- 根因：`data.py` 把腾讯 `f[74]` 直接当「资产负债率」，实为**负债/净资产类杠杆率**（净负债口径可为负）：映美控股 5965.22%（净资产≈0 分母趋零爆炸，东财每股净资产 -0.086 为负）、腾讯 -28.41（净现金）、东亚银行 51.52（银行真实负债率约 90%）。
- 处置：移除 `f[74]→debt_ratio` 映射，港股负债率标**数据缺失**（fail-closed）；渲染层加 0-100% 护栏兜底。
- 测试：新增 `test_tencent_hk_quote_debt_ratio_removed` / `test_render_profile_line_hk_debt_ratio_range_guard`，全量 **108 通过**。

### 3. Skill 分发重构（commit `c71f16d`）— GitHub 下载即用
- **semantica 转 optional extra**：默认 `uv sync` 轻量（几十 MB），完整 RETE 影子裁决 `uv sync --extra semantica`（约 2.5GB，含 torch/faiss）；缺失自动降级纯 Python。
- 新增 `scripts/install_skill.sh` / `install_skill.ps1`：一键安装为 AI Skill（Claude Code / Codex / ZCode，`--dest` 指定目录），curl 远程模式 + 本地仓库两种路径，排除 `__pycache__`。
- 仓库清理：`data/`（AgentMemory 记忆库，隐私）、`report/` 历史报告、`ve.pptx`（13MB）出库。

### 4. 文档重写（commit `72e505c`）
- [README](../../README.md) 重写为面向人：**零成本 · A股+港股 量化选股 AI Skill**，纯免费数据源、对话示例、FAQ 合并。
- [AGENTS.md](../../AGENTS.md) 同步：项目定位、semantica optional、文件清单补 install_skill.sh。

### 5. 发布 V1.7（tag `V1.7`）
- annotated tag + GitHub Release 已发布：https://github.com/xiazhicheng/quant-risk/releases/tag/V1.7
- Docker 镜像 CI 自动构建成功（GHCR，含 semantica 全量）。

### 6. 移除 openwiki（commit `454a306`）
- 用户反馈 openwiki 不好用：删除 `openwiki/`、`.okignore`、`.ok/`、`.agents/skills/open-knowledge/` 及全部引用。

### 7. OpenKnowledge 接入 Kimi Code（commit `6f6f781`）
- 新建 `.kimi-code/mcp.json`：`open-knowledge` stdio server（`ok mcp`，继承项目根 cwd，跨机器可移植）。
- `ok init` 重建 `.ok/`（config.yml 入库，local/ 等运行时状态由 `.ok/.gitignore` 排除）并重装项目级 skill（.claude/.cursor/.agents）。
- MCP 实测：initialize 握手成功（open-knowledge v0.76.0），暴露工具 `exec/search/write/edit`。
- ⚠️ Kimi Code mid-session 不注册新 MCP server——**新会话生效**。

## 二、关键决策

| 决策 | 依据 |
|:----|:----|
| 港股负债率标数据缺失 | f[74] 口径错误且无可靠免费源，fail-closed 优于错误数字 |
| semantica 转 optional | 官方全量 2.5GB 劝退「下载即用」，实际只用纯标准库 RETE |
| Skill 安装为第一分发顺位 | 目标用户是 AI 用户，对话即用 > CLI 安装 |
| 知识库目录用默认项目根 | `.ok/config.yml` 的 `content.dir` 未配置，项目根 md 即 OK 文档 |

## 三、验证

- ✅ `uv run pytest tests/ -q`：108 passed（轻量环境 106+2skip / semantica 环境 108）
- ✅ `ok mcp` stdio 握手：`open-knowledge v0.76.0`，tools: exec/search/write/edit
- ✅ `install_skill.sh` 本地/远程/`--dest` 三种模式冒烟通过
- ✅ 远端 `origin/main` 与本地一致（`6f6f781`）

## 四、相关文档

- [AGENTS.md](../../AGENTS.md) — 项目约定与设计决策（含本日全部变更记录）
- [README.md](../../README.md) — 用户使用说明（零成本 Skill 定位）
- [SKILL.md](../../SKILL.md) — AI Skill 主定义（V2.0）
